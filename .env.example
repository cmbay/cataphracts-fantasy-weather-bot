# Discord webhook URL for weather forecast (use regions.json for local dev regions)
WEEKLY_FORECAST_WEBHOOK_URL=https://discord.com/api/webhooks/YOUR_WEEKLY_FORECAST_WEBHOOK_ID/YOUR_WEEKLY_FORECAST_WEBHOOK_TOKEN

# Google Sheets Integration (optional - for dynamic channel configuration)
# If these are set, channel and assignment configs will be loaded from Google Sheets
# Otherwise, falls back to local JSON files (channels.json, channel-assignments.json)
GOOGLE_SERVICE_ACCOUNT_KEY=BASE64_ENCODED_SERVICE_ACCOUNT_KEY_JSON
GOOGLE_SPREADSHEET_ID=YOUR_GOOGLE_SPREADSHEET_ID